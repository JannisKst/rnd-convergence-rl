# Byte-compiled / caches
__pycache__/
*.py[cod]
.ruff_cache/
.mypy_cache/
.pytest_cache/
.ipynb_checkpoints

# Packaging
build/
dist/
*.egg-info/

# Environments
.env
.venv/
venv/
uv.lock

# Hydra / experiment outputs. Anchored to the repository root: these patterns name the
# raw output directories, and an unanchored `results/` also swallows docs/results/, which
# holds the curated frame and curves the report is written from and must be committed.
/outputs/
/multirun/
/results/
/logs/
/wandb/
/runs/

# Model weights & large artifacts
*.pt
*.pth
*.ckpt

# OS / editor
.DS_Store
.vscode/
.idea/

CLAUDE.md
