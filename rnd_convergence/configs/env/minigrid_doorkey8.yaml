# Rung 3, the sparse-reward case: (x, y, dir), 3-D discrete.
#
# DoorKey-8x8 is built to defeat agents without an exploration bonus, so PPO is expected
# *not* to solve it. That is the point: it is the decoupling case, where coverage
# saturates while return never converges, and t_conv is legitimately `not_learned`. A
# generous budget is still worth spending --- "did not learn in 300k steps" is a much
# weaker statement than "did not learn in 500k".
id: MiniGrid-DoorKey-8x8-v0
kwargs: {}

# 10 evaluation episodes, matching minigrid_empty and for the same measured reason: extra
# episodes buy nothing here. The within-evaluation standard deviation is exactly zero at all
# 100 evaluation points, because the agent never reaches the goal and every episode returns 0
# whatever layout it drew. Averaging more zeros does not make the number more precise.
total_steps: 500_000
eval_interval: 5_000
eval_episodes: 10
rollout_steps: 2_048

signal_window: 10_000
signal_stride: 5_000
